//   Read the documentation to learn more about C++ code generator
//   versioning.
//	  %X% %Q% %Z% %W%
//	/local/data/veris2/dorman/cpp/xspec_cxx/model

#ifndef MODEL_FUNCTIONS_H
#define MODEL_FUNCTIONS_H 1



#endif


// Detached code regions:
// WARNING: this code will be lost if code is regenerated.
#if 0
  // requires that T has a dataGroupNumber and a sourceNumber element
  // or will fail at link time.
  if (right.name() !=  left.name() )
  {
        return  left.name() < right.name();       
  }
  else
  {
        return  left.dataGroupNumber() < right.dataGroupNumber();      
  }
  bool result(false);
  if (right.name() ==  left.name() )
  {
        result = (left.dataGroupNumber() ==  right.dataGroupNumber());        
  }
  return result;  

#endif
